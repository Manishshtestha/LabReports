#include <stdio.h>
#include <string.h>

int main(){
    char str[50];
    printf("Enter a random string:");
    scanf("%s",str);
    printf("The length of the given string is %d",strlen(str));
    return 0;
}//main


